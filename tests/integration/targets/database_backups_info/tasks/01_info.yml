# code: language=ansible
---
- name: 01_info | Get database_backups_info
  digitalocean.cloud.database_backups_info:
    token: "{ digitalocean_token }"
    cluster_id: "{ database_cluster_id }"
  register: result

- name: 01_info | Verify database_backups_info result
  ansible.builtin.assert:
    that:
      - not result.changed
      - result.msg is string
