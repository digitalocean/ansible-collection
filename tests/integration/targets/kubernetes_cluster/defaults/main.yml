kubernetes_cluster:
  name: "ansible-gh-ci-k8s-{{ pr_number | default(0, true) }}"
  region: nyc3
  version: "1.34.1-do.2"
  node_pools:
    - size: s-2vcpu-4gb
      name: ansible-gh-ci-k8s-pool-0
      count: 1
