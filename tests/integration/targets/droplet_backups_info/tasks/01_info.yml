---
- name: 01_info | Get droplet_backups_info
  digitalocean.cloud.droplet_backups_info:
    token: "{{ digitalocean_token }}"
    droplet_id: "{{ droplet_id }}"
  register: result

- name: 01_info | Verify droplet_backups_info result
  ansible.builtin.assert:
    that:
      - not result.changed
      - result.msg is string
