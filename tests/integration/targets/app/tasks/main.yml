---
- block:

    - name: Verify API token
      ansible.builtin.assert:
        that:
          - digitalocean_token is string
          - digitalocean_token | length > 0
        fail_msg: digitalocean_token should be defined in tests/integration/integration_config.yml

    - name: Verify App Platform repo
      ansible.builtin.assert:
        that:
          - app_repo is string
          - app_repo | length > 0
        fail_msg: app_repo must be set in defaults or extra vars

    - name: Test app | Create, update, and delete
      ansible.builtin.include_tasks:
        file: 01_create_update_delete.yml
